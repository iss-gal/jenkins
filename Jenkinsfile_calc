def first = 10
def second = 20
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Addition"
                a=$((first + second)
                echo a;
            }
        }
    }
}
